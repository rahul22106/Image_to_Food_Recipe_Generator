# Core Dependencies
python-dotenv
ensure
types-PyYAML

# AI/ML
# (torch, torchvision installed in Dockerfile)
transformers
sentence-transformers
accelerate
sentencepiece
groq
google-generativeai

# LangChain
langchain
langchain-community
langchain-core
langchain-groq
langchain-google-genai
faiss-cpu
chromadb

# Image Processing
# (Pillow, opencv-python-headless installed in Dockerfile)
opencv-python

# Web Framework
# (fastapi, uvicorn, python-multipart, pydantic, python-box, pyyaml installed in Dockerfile)
jinja2

# Data Processing
# (numpy, pandas, scikit-learn installed in Dockerfile)

# Database
pymongo

# Utilities
# (requests, boto3, tqdm installed in Dockerfile)
aiofiles
python-jose[cryptography]
httpx
orjson

# Testing
pytest
pytest-cov
pytest-asyncio
flake8

# Logging
loguru

# Data Validation
pydantic-settings

# Frontend
streamlit